<% if user_signed_in? %>
  
  <%= link_to('Edit registration', edit_user_registration_path, class: "btn btn-medium btn-primary" ) %>
  
<% else %>
  
  <%= link_to('Register', new_user_registration_path, class: "btn btn-medium btn-primary" )  %>
  
<% end %>