<% if user_signed_in? %>
  
  <%= link_to('Logout', destroy_user_session_path, :method => :delete, class: "btn btn-medium btn-primary"  ) %>        
  
<% else %>

  <%= link_to('Login', new_user_session_path, class: "btn btn-medium btn-primary" )  %>  
 
<% end %>